{
  "name": "polyrunner",
  "version": "0.1.1",
  "description": "TUI microservice manager",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "start": "cargo run --release"
  }
}